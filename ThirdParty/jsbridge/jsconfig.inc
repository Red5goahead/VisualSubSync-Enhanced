(*
 * Some conditional defines:
 *
 *  JSUnicode
 *      Enables Unicode support throughout the bridge classes.
 *
 *  JSDebug
 *      Enables more detailed debugging within the SpiderMonkey DLL.  Since the
 *      debug DLL is not distributed (for size reasons), please contact Sterling
 *      at whoelse@sterlingbates.com for a copy.
 *)

{$DEFINE JSUnicode}
{.$DEFINE JSDebug}
{.$DEFINE JSDEBUGGER}
